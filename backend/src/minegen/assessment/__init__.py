"""Phase 20D.3 — Design Assessment & Candidate Comparison (rule 189).

A READ-ONLY projection of existing authoritative design results
(``layout_v2.json``, ``layout_v2_selected.json``, ``ramp_source.json``,
``capability_graph.json``) into typed engineering checks and a candidate
comparison. It never creates geometry, changes feasibility, re-ranks
candidates, recomputes a score or makes a statutory claim, and it persists
nothing (a read model computed per request from one validated snapshot).
"""
